import React from 'react';
import HeaderContainer from '../Containers/HeaderContainer';

const Header = () => {
    return (
        <div>
            <HeaderContainer/>
        </div>
    );
};

export default Header;